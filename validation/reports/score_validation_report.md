# Score validation report

No external score annotations available. Validation status: **verification only**.

Toy fixtures in `validation/score_annotations/examples/` are for schema tests only and must not be treated as empirical validation results.

Regenerate this report with:

```bash
python validation/scripts/correlate_metrics_with_ratings.py validation/score_annotations/examples/toy_fixture.json
```
