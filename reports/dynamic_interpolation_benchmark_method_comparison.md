# Benchmark method comparison

Diagnostic only; frozen outputs unchanged.

> **Pre-change note (2026-08-18).** Same caveat as
> `dynamic_interpolation_method_comparison.md`: this report's PCHIP column
> is dynamic-axis (3 source anchors), not pitch-table `MIN_PCHIP_ANCHORS`.
> Not regenerated.
